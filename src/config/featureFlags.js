// Feature flags for unreleased home-screen blocks. Default false until the
// upstream content pipeline (Sharp Journal, evening edition, Field Guide
// backlog) lands. Flip to true here to enable across the app.
export const FEATURE_EVAN_COLE_READ = false;
export const FEATURE_DISCIPLINE_ARTICLES = false;
export const FEATURE_EVENING_RECAP = false;
